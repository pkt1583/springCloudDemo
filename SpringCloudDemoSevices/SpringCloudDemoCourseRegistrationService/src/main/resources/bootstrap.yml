spring:
  application:
    name: courseStudentRegistration
  cloud:
    config:
      discovery:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:12345/eureka